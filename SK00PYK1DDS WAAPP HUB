repeat task.wait() until game:IsLoaded()

-- [[ CONFIGURATION ]]
local correctGameId = 192800 
local kickMessage = "⚠️ Sorry Bro :d You Are Not On The Correct Game. (SK00PYK1DD HUBS)"
local rawLink = "https://raw.githubusercontent.com/SK00PYK1DDSHUBS/SK00PYK1DDS-WAAPP-HUB/refs/heads/main/SK00PYK1DDS%20WAAPP%20HUB"
-- [[ GAME GUARD ]]
if game.PlaceId ~= correctGameId and game.GameId ~= correctGameId then
	game:GetService("Players").LocalPlayer:Kick(kickMessage)
	return
end

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local TeleportService = game:GetService("TeleportService")
local player = Players.LocalPlayer

-- Cleanup old UI before starting
pcall(function()
	if CoreGui:FindFirstChild("SK00PYHub") then
		CoreGui.SK00PYHub:Destroy()
	end
end)

local gui = Instance.new("ScreenGui")
gui.Name = "SK00PYHub"
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = gethui and gethui() or CoreGui

-- 🌱 COLORS
local mainColor = Color3.fromRGB(18, 32, 18)
local topColor = Color3.fromRGB(10, 24, 10)
local accent = Color3.fromRGB(80, 255, 120)
local accentDark = Color3.fromRGB(45, 180, 80)
local buttonColor = Color3.fromRGB(28, 50, 28)
local strokeColor = Color3.fromRGB(90, 255, 140)

-- MAIN FRAME
local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0,650,0,400)
main.Position = UDim2.new(0.5,-325,0.5,-200)
main.BackgroundColor3 = mainColor
main.Active = true
main.Draggable = true
Instance.new("UICorner", main).CornerRadius = UDim.new(0,14)
Instance.new("UIStroke", main).Color = strokeColor

-- TOPBAR
local topbar = Instance.new("Frame", main)
topbar.Size = UDim2.new(1,0,0,55)
topbar.BackgroundColor3 = topColor
Instance.new("UICorner", topbar).CornerRadius = UDim.new(0,14)

local title = Instance.new("TextLabel", topbar)
title.BackgroundTransparency = 1
title.Position = UDim2.new(0,20,0,0)
title.Size = UDim2.new(0,450,1,0)
title.Font = Enum.Font.GothamBlack
title.Text = "SK00PYK1DD’s Waapp Hub"
title.TextColor3 = accent
title.TextScaled = true
title.TextXAlignment = Enum.TextXAlignment.Left

-- CLOSE BUTTON
local close = Instance.new("TextButton", topbar)
close.Size = UDim2.fromScale(0.055, 0.7)
close.Position = UDim2.fromScale(0.92, 0.15)
close.TextScaled = true
close.Text = "X"
close.Font = Enum.Font.GothamBold
close.TextColor3 = Color3.new(1,1,1)
close.BackgroundColor3 = Color3.fromRGB(40,120,40)
Instance.new("UICorner", close).CornerRadius = UDim.new(0,10)
close.MouseButton1Click:Connect(function() gui:Destroy() end)

-- CATEGORY BAR
local categoryBar = Instance.new("Frame", main)
categoryBar.Position = UDim2.new(0,15,0,70)
categoryBar.Size = UDim2.new(1,-30,0,45)
categoryBar.BackgroundColor3 = buttonColor
Instance.new("UICorner", categoryBar).CornerRadius = UDim.new(0,10)

local function createTab(name, index)
	local btn = Instance.new("TextButton", categoryBar)
	btn.Size = UDim2.new(0, 145, 0.8, 0) 
	btn.Position = UDim2.new(0, 10 + ((index-1) * 153), 0.1, 0)
	btn.BackgroundColor3 = buttonColor
	btn.Text = name
	btn.Font = Enum.Font.GothamBold
	btn.TextScaled = true 
	btn.TextColor3 = Color3.new(1,1,1)
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)
	local constraint = Instance.new("UITextSizeConstraint", btn)
	constraint.MaxTextSize = 18
	constraint.MinTextSize = 12
	return btn
end

local teleportTab = createTab("📍 Teleports", 1)
local switchTab = createTab("💼 Jobs", 2)
local musicTab = createTab("🎵 Music", 3)
local localPlayerTab = createTab("👤 Player", 4)

-- HOLDERS
local function createHolder()
	local h = Instance.new("ScrollingFrame", main)
	h.Position = UDim2.new(0,15,0,130)
	h.Size = UDim2.new(1,-30,1,-145)
	h.BackgroundTransparency = 1
	h.ScrollBarThickness = 6
	h.ScrollBarImageColor3 = accent
	h.CanvasSize = UDim2.new(0,0,0,0)
	h.AutomaticCanvasSize = Enum.AutomaticSize.Y
	h.BorderSizePixel = 0
	h.Visible = false
	
	local grid = Instance.new("UIGridLayout", h)
	grid.CellSize = UDim2.new(0,285,0,65)
	grid.CellPadding = UDim2.new(0,10,0,10)
	
	local pad = Instance.new("UIPadding", h)
	pad.PaddingTop = UDim.new(0,5)
	pad.PaddingBottom = UDim.new(0,10)
	return h
end

local teleportHolder = createHolder()
teleportHolder.Visible = true
teleportTab.BackgroundColor3 = accentDark

local switchHolder = createHolder()
local musicHolder = createHolder()
local localPlayerHolder = createHolder()

-- BUTTON CREATORS
local function createGenericBtn(parent, name, callback)
	local b = Instance.new("TextButton", parent)
	b.BackgroundColor3 = buttonColor
	b.Text = ""
	Instance.new("UICorner", b).CornerRadius = UDim.new(0,12)
	local t = Instance.new("TextLabel", b)
	t.Parent = b
	t.BackgroundTransparency = 1
	t.Size = UDim2.new(1,-20,1,0)
	t.Position = UDim2.new(0,10,0,0)
	t.Font = Enum.Font.GothamBold
	t.Text = name
	t.TextColor3 = accent
	t.TextScaled = true
	t.TextXAlignment = Enum.TextXAlignment.Left
	b.MouseButton1Click:Connect(callback)
end

-- DATA POPULATION
local jobAreas = {{"Manager Office", Vector3.new(26,7,6)},{"Pizza Boxing Area", Vector3.new(55,4,12)},{"Cook Area", Vector3.new(36,4,58)},{"Cashier Area", Vector3.new(49,4,83)},{"Delivery Area", Vector3.new(66,4,-16)},{"Supplier Area", Vector3.new(7,13,-1021)}}
local extras = {{"The Dump", Vector3.new(23,3,-182)},{"The Pet Shop", Vector3.new(28,3,-242)},{"Island 1", Vector3.new(1523,0,1363)},{"Island 2", Vector3.new(-991,34,714)},{"Island 3", Vector3.new(-1763,100,-1324)},{"Boat Area", Vector3.new(-469,2,-341)},{"Revive Pets", Vector3.new(-1748,2,-1262)},{"Secret Area", Vector3.new(-481,-24,-544)}}

for _, data in ipairs(jobAreas) do createGenericBtn(teleportHolder, data[1], function() player.Character.HumanoidRootPart.CFrame = CFrame.new(data[2]) end) end
for _, data in ipairs(extras) do createGenericBtn(teleportHolder, data[1], function() player.Character.HumanoidRootPart.CFrame = CFrame.new(data[2]) end) end

local jobs = {{"Cashier","Cashier"},{"Cook","Cook"},{"Pizza Boxer","Pizza Boxer"},{"Delivery","Delivery"},{"Supplier","Supplier"}}
for _,v in ipairs(jobs) do createGenericBtn(switchHolder, "Switch To "..v[1], function() game:GetService("ReplicatedStorage").PlayerChannel:FireServer("ChangeJob", v[2]) end) end

createGenericBtn(musicHolder, "The Dump Music", function() game:GetService("ReplicatedStorage").PlayerChannel:FireServer("LocationChanged", "TheDump") end)
createGenericBtn(musicHolder, "The Pet Store Music", function() game:GetService("ReplicatedStorage").PlayerChannel:FireServer("LocationChanged", "PetShop") end)

createGenericBtn(localPlayerHolder, "Super Speed", function() player.Character.Humanoid.WalkSpeed = 100 end)
createGenericBtn(localPlayerHolder, "Super Jump", function() player.Character.Humanoid.JumpPower = 200 end)
createGenericBtn(localPlayerHolder, "Sit", function() player.Character.Humanoid.Sit = true end)
createGenericBtn(localPlayerHolder, "Respawn", function()
	local lastPos = player.Character.HumanoidRootPart.CFrame
	player.Character.Humanoid.Health = 0
	player.CharacterAdded:Wait()
	player.Character:WaitForChild("HumanoidRootPart").CFrame = lastPos
end)

-- THE REJOIN BUTTON xd xd
createGenericBtn(localPlayerHolder, "Rejoin (Auto Loads The Hub Too)", function()
	local qot = (syn and syn.queue_on_teleport) or queue_on_teleport or (fluxus and fluxus.queue_on_teleport)
	
	if qot then
		-- This line queues the loader
		qot([[loadstring(game:HttpGet("]]..rawLink..[["))()]])
	end
	
	-- Teleport Logic
	if #Players:GetPlayers() <= 1 then
		TeleportService:Teleport(game.PlaceId, player)
	else
		TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, player)
	end
end)

-- TAB SWITCH LOGIC
local function switchMenu(activeTab, activeHolder)
	teleportTab.BackgroundColor3 = buttonColor
	switchTab.BackgroundColor3 = buttonColor
	musicTab.BackgroundColor3 = buttonColor
	localPlayerTab.BackgroundColor3 = buttonColor
	teleportHolder.Visible = false
	switchHolder.Visible = false
	musicHolder.Visible = false
	localPlayerHolder.Visible = false
	
	activeTab.BackgroundColor3 = accentDark
	activeHolder.Visible = true
end

teleportTab.MouseButton1Click:Connect(function() switchMenu(teleportTab, teleportHolder) end)
switchTab.MouseButton1Click:Connect(function() switchMenu(switchTab, switchHolder) end)
musicTab.MouseButton1Click:Connect(function() switchMenu(musicTab, musicHolder) end)
localPlayerTab.MouseButton1Click:Connect(function() switchMenu(localPlayerTab, localPlayerHolder) end)
